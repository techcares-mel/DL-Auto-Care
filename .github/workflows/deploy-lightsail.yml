name: Deploy Theme to Lightsail Staging

on:
  push:
    branches: [staging]
    paths:
      - 'wordpress-theme/**'
      - '.github/workflows/deploy-lightsail.yml'

jobs:
  lint:
    name: PHP Syntax Check
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: '8.1'

      - name: Check PHP syntax
        run: |
          echo "Checking PHP syntax in wordpress-theme/..."
          find wordpress-theme -name "*.php" -print0 | xargs -0 -n1 php -l
          echo "✅ All PHP files passed syntax check."

  deploy:
    name: Deploy via SCP to Lightsail
    runs-on: ubuntu-latest
    needs: lint
    steps:
      - uses: actions/checkout@v4

      - name: Copy theme files to home directory (temp)
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.LIGHTSAIL_HOST }}
          username: ${{ secrets.LIGHTSAIL_USER }}
          key: ${{ secrets.LIGHTSAIL_KEY }}
          port: 22
          source: "wordpress-theme/*"
          target: "/home/bitnami/dl-auto-care-tmp/"
          strip_components: 1

      - name: Move files into WordPress themes directory and fix permissions
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.LIGHTSAIL_HOST }}
          username: ${{ secrets.LIGHTSAIL_USER }}
          key: ${{ secrets.LIGHTSAIL_KEY }}
          port: 22
          script: |
            sudo mkdir -p /opt/bitnami/wordpress/wp-content/themes/dl-auto-care/
            sudo cp -r /home/bitnami/dl-auto-care-tmp/. /opt/bitnami/wordpress/wp-content/themes/dl-auto-care/
            sudo chown -R bitnami:daemon /opt/bitnami/wordpress/wp-content/themes/dl-auto-care/
            find /opt/bitnami/wordpress/wp-content/themes/dl-auto-care/ -type f -exec sudo chmod 644 {} \;
            find /opt/bitnami/wordpress/wp-content/themes/dl-auto-care/ -type d -exec sudo chmod 755 {} \;
            rm -rf /home/bitnami/dl-auto-care-tmp/
            echo "✅ Deploy complete."
